server:
  port: 8082

spring:
  application:
    name: core

  datasource:
    url: jdbc:mysql://localhost:3302/CORE?serverTimezone=UTC&characterEncoding=UTF-8
    username: rlatkd
    password: '0000'
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        physical-strategy: org.hibernate.boot.model.naming.CamelCaseToUnderscoresNamingStrategy
    open-in-view: false
